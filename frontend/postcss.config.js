export default {
  plugins: {
    // Scans your HTML/JSX files and compiles the Tailwind utility classes you used into raw CSS
    tailwindcss: {},
    
    // Automatically adds vendor prefixes (like -webkit- or -moz-) to your CSS rules to ensure older browsers render your styles correctly
    autoprefixer: {},
  },
};
